{
  "name": "@newo-dsl/data",
  "version": "0.5.3",
  "description": "Shared data definitions for Newo DSL (actions, builtins, validation rules)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "echo \"Tests run from root via vitest\""
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist"
  ]
}
