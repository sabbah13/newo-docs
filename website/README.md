### Newo SuperAgent Docs (Docusaurus)

Commands:

- Install: `npm install`
- Start dev server: `npm run start`
- Build: `npm run build`
- Serve build: `npm run serve`

This site serves content from `../docs` via the classic preset.
