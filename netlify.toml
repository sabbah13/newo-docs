# Netlify configuration for Newo SuperAgent Documentation
[build]
  # Build command - npm commands run from website directory
  command = "npm ci && npm run build"
  
  # Directory to deploy (relative to website directory)
  publish = "build"
  
  # Base directory for build context
  base = "website"
  
  # Node.js version
  environment = { NODE_VERSION = "18" }

# Redirect rules for clean URLs and SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Branch-specific build settings
[context.production]
  command = "npm ci && npm run build"

[context.branch-deploy]
  command = "npm ci && npm run build"

[context.deploy-preview]
  command = "npm ci && npm run build"

# Environment variables (set these in Netlify UI)
# NODE_VERSION = "18"
# NPM_VERSION = "9"